version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: testdb
    volumes:
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - backend_net

  backend:
    build: ./backend
    depends_on:
      - db
    networks:
      - backend_net
      - app_net

  frontend:
    build: ./frontend
    networks:
      - app_net

  gateway:
    build: ./gateway
    ports:
      - "8080:80"
    depends_on:
      - frontend
      - backend
    networks:
      - app_net

networks:
  backend_net:
  app_net:
